<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Contacts.Views.AddContactPage"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    ios:Page.UseSafeArea="true">
    <ContentPage.Content>
            <Grid  Margin="10,0,10,0" VerticalOptions="CenterAndExpand">
                <Grid.RowDefinitions>
                  <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
                <RowDefinition Height="80"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Entry  Placeholder="Name" Style="{StaticResource Key=entryStyle}"
                         HeightRequest="90" Grid.Row="2" Text="{Binding nContact.Name}"
                        HorizontalOptions="FillAndExpand" VerticalOptions="Center"  />
                <Entry  Placeholder="Number" Style="{StaticResource Key=entryStyle}" Text="{Binding nContact.Number}"
                         HeightRequest="90" HorizontalOptions="FillAndExpand" VerticalOptions="Center"
                        Grid.Row="3" />
            <Button Text="Add" Command="{Binding AddCommand}" Style="{StaticResource Key=buttonStyle}" Grid.Row="4"/>
            </Grid>
    </ContentPage.Content>
</ContentPage>
